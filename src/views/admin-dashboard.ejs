<%- include('partials/head', { title: 'Dashboard' }) %>
<%- include('partials/nav', { active: 'dashboard' }) %>

<div class="container">
  <div class="range-select">
    <label>
      Range:
      <select id="range-select">
        <option value="7">Last 7 days</option>
        <option value="30" selected>Last 30 days</option>
        <option value="90">Last 90 days</option>
      </select>
    </label>
  </div>

  <div class="stat-tiles">
    <div class="stat-tile">
      <div class="value" id="stat-all-time">–</div>
      <div class="label">All-time scans</div>
    </div>
    <div class="stat-tile">
      <div class="value" id="stat-7d">–</div>
      <div class="label">Scans, last 7 days</div>
    </div>
    <div class="stat-tile">
      <div class="value" id="stat-30d">–</div>
      <div class="label">Scans, last 30 days</div>
    </div>
    <div class="stat-tile">
      <div class="value" id="stat-active">–</div>
      <div class="label">Active QR codes</div>
    </div>
    <div class="stat-tile">
      <div class="value" id="stat-unique">–</div>
      <div class="label">Unique devices (range)</div>
    </div>
    <div class="stat-tile">
      <div class="value" id="stat-repeat">–</div>
      <div class="label">Repeat scans (range)</div>
    </div>
  </div>

  <div class="card">
    <h3>Scans over time (all QR codes combined)</h3>
    <div style="height:260px">
      <canvas id="chart-timeline"></canvas>
    </div>
  </div>

  <div class="charts-grid">
    <div class="card">
      <h3>Scans per QR code</h3>
      <div style="height:220px">
        <canvas id="chart-leaderboard"></canvas>
      </div>
    </div>
    <div class="card">
      <h3>Device type</h3>
      <div style="height:220px">
        <canvas id="chart-device"></canvas>
      </div>
    </div>
  </div>

  <div class="card">
    <h3>QR code leaderboard</h3>
    <table>
      <thead>
        <tr>
          <th>Label</th>
          <th>Scans</th>
          <th>Unique devices</th>
          <th>Repeat scans</th>
          <th>Last scanned</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody id="leaderboard-body">
        <tr><td colspan="6">Loading…</td></tr>
      </tbody>
    </table>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js@4"></script>
<script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels@2"></script>
<script src="/dashboard.js"></script>
<script>
  initDashboard('/api/stats/combined');
</script>
</body>
</html>
